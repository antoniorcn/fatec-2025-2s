spring.application.name=agenda-contato

# Configurar variaveis e ambiente no command prompt
# SET DB_DATABASE=agendaitq
# SET DB_USERNAME=root
# SET DB_PASSWORD=123456
# SET DB_URL=mariadb://localhost:3306
# SET JWT_SECRET=ABC12345678
# SET MQTT_URI=tcp://192.168.15.2:1883

jwt.secret=${JWT_SECRET}

mqtt.uri=${MQTT_URI}

spring.datasource.url=jdbc:${DB_URL}/${DB_DATABASE}?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.error.include-stacktrace=never